FROM php:7.1-fpm

RUN apt-get update && apt-get install -y libmcrypt-dev mariadb-server \
    mariadb-client && docker-php-ext-install mcrypt pdo_mysql 

RUN mkdir /docker-entrypoint-initdb.d

WORKDIR /var/www